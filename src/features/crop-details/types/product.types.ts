export interface Product {
  id: string;
  date: string;
  type: string;
  name: string;
  quantity: string;
}

export interface ProductFormData {
  type: string;
  name: string;
  quantity: string;
}
